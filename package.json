{
  "name": "worldmappaths",
  "version": "1.0.0",
  "description": "Shows the shortest path to your destination.",
  "type": "module",
  "scripts": {
    "release": "cross-env ENVIRONMENT=deploy node ./utils/build.js",
    "build": "cross-env ENVIRONMENT=development node ./utils/build.js",
    "watch-dev": "watch \"npm run build\" ./src",
    "update-version": "powershell ./utils/updateversion.ps1"
  },
  "author": "AnotherORC",
  "license": "ISC",
  "devDependencies": {
    "@alumna/reflect": "^1.1.3",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "watch": "^1.0.2",
    "zip-a-folder": "^3.1.6"
  }
}